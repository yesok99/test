javascript:(function() {   var getKey = function() {const table = document.querySelector('table');const rows = table.querySelectorAll('tr');const lastColumnData = [];rows.forEach(row => {  const cells = row.querySelectorAll('td');  if( cells.length == 4) {      const lastCellContent = cells[cells.length - 1].textContent;  lastColumnData.push(lastCellContent);}});console.log(lastColumnData);    return JSON.stringify(lastColumnData);    };    let text = getKey();      var maskDiv = document.createElement('div');    maskDiv.style.position = 'fixed';    maskDiv.style.top = '0';    maskDiv.style.left = '0';    maskDiv.style.width = '100%';    maskDiv.style.height = '100%';    maskDiv.style.backgroundColor = 'rgba(0, 0, 0, 0.5)';     document.body.appendChild(maskDiv);    var mainDiv = document.createElement('div');    mainDiv.style.width = '600px';    mainDiv.style.height = '80%';    mainDiv.style.position = 'fixed';    mainDiv.style.top = '50%';    mainDiv.style.left = '50%';    mainDiv.style.transform = 'translate(-50%, -50%)';    mainDiv.style.border = '2px solid green';    mainDiv.style.background = '#fff';        var button = document.createElement('button');    button.innerText = '删除';    button.addEventListener('click', function() {      document.body.removeChild(maskDiv);      document.body.removeChild(mainDiv);    });        var textDiv = document.createElement('textarea');    textDiv.style.position = 'relative';    textDiv.style.overflowY = 'scroll';    textDiv.style.height = '90%';    textDiv.style.width = '100%';    textDiv.style.padding = '20px';    textDiv.style.whiteSpace = "pre-wrap";    textDiv.setAttribute('readonly', 'readonly');var copyButton = document.createElement('button');    copyButton.innerText = '复制';    copyButton.addEventListener('click', function() {      navigator.clipboard.writeText(textDiv.value).then(function() {        alert('复制成功！');      }).catch(function(err) {        alert('复制失败：' + err);      });    });            var buttons = document.createElement('div');    buttons.style.position = 'relative';    buttons.style.padding = '10px';buttons.style.textalign = 'center';buttons.style.display = "flex";    buttons.style.justifyContent = "center";    buttons.style.alignItems = "center";    textDiv.value = text;        document.body.appendChild(mainDiv);    buttons.appendChild(copyButton);    buttons.appendChild(button);    mainDiv.appendChild(textDiv);    mainDiv.appendChild(buttons);  })();